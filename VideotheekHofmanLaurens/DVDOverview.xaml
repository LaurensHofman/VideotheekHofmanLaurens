<UserControl x:Name="wndDVDOverview" 
    x:Class="VideotheekHofmanLaurens.DVDOverview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VideotheekHofmanLaurens"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Background="White">
    <Grid>
        <DataGrid Name="grdDVDOverview" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=Name}" Header="Name" />
                <DataGridTextColumn Binding="{Binding Path=Description}" Width="*" Header="Description" />
                <DataGridTextColumn Binding="{Binding Path=Director}" Header="Director" />
                <DataGridTextColumn Binding="{Binding Path=Genres}" Header="Genres" />
                <DataGridTextColumn Binding="{Binding Path=PEGIage}" Header="PEGI" />
                <DataGridTextColumn Binding="{Binding Path=Stock}" Header="Current Stock" />
                <DataGridTextColumn Binding="{Binding Path=Price}" Header="Price" />
                <DataGridTextColumn Binding="{Binding Path=DVDType}" Header="DVD Type" />
                <DataGridTextColumn Binding="{Binding Path=MovieDuration}" Header="Duration (minutes)" />
                <DataGridTextColumn Binding="{Binding Path=SeriesEpisodes}" Header="Episodes" />
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button x:Name="rbtnUpdateDVD" Content="Update" Click="rbtnUpdateDVD_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button x:Name="rbtnDeleteDVD" Content="Delete" Click="rbtnDeleteDVD_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
